model User {
    id        String   @id @default(uuid()) @db.Uuid
    name      String
    icon      String
    isDefault Boolean
    createdAt DateTime @default(now())
    updatedAt DateTime @default(now())
    // scores    Score[]
    // chips     Chip[]
    // rooms     RoomUser[]
}
