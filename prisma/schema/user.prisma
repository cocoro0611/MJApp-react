model User {
    id        String   @id @default(uuid()) @db.Uuid
    name      String
    icon      String
    createdAt DateTime @default(now())
    updatedAt DateTime @default(now())
    // scores    Score[]
    // chips     Chip[]
    // rooms     RoomUser[]
}
